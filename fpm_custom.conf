; Phone photos are several MB each and a capture can carry three, so the stock
; 2M upload / 8M post limits reject a normal item outright.
php_value[memory_limit] = 256M
php_value[post_max_size] = 100M
php_value[upload_max_filesize] = 100M
