# Symfony front-controller routing for the heroku-php-nginx buildpack.
location / {
    try_files $uri /index.php$is_args$args;
}

# Uploads live in object storage; nothing under these is web-servable.
location ~ ^/(var|config|src|templates|migrations)/ {
    deny all;
}
